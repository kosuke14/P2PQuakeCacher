{
  "name": "p2pquakecacher",
  "version": "1.0.0",
  "description": "A backend API that caches p2pquake's datas not to hit their rate limits, and proxies them for the clients which does not support WebSocket.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^17.0.1",
    "express": "^5.1.0",
    "ws": "^8.18.3"
  }
}
